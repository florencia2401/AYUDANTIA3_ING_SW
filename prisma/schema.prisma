generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Modelo de Categoría
model Category {
  id          Int       @id @default(autoincrement())
  name        String    @unique @db.VarChar(80)
  description String?   @db.Text
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  // Relación 1:N con Productos (Una categoría tiene muchos productos)
  products    Product[]

  @@map("categories")
}

// Modelo de Producto
model Product {
  id          Int      @id @default(autoincrement())
  name        String   @db.VarChar(150)
  description String?  @db.Text
  price       Float
  stock       Int      @default(0)
  sku         String   @unique @db.VarChar(50)
  isAvailable Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relación N:1 con Categoría (Cada producto pertenece a una categoría)
  categoryId  Int
  category    Category @relation(fields: [categoryId], references: [id], onDelete: Restrict)

  // Relación N:1 con Marca (Cada producto pertenece opcionalmente a una marca)
  brandId     Int?
  brand       Brand?   @relation(fields: [brandId], references: [id], onDelete: SetNull)

  // Relación 1:N con Reseñas (Un producto tiene muchas reseñas)
  reviews     Review[]

  @@map("products")
}

// Modelo de Marca
model Brand {
  id        Int      @id @default(autoincrement())
  name      String   @unique @db.VarChar(80)
  country   String?  @db.VarChar(60)
  website   String?  @db.VarChar(200)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relación 1:N con Productos (Una marca tiene muchos productos)
  products  Product[]

  @@map("brands")
}

// Modelo de Reseña
model Review {
  id        Int      @id @default(autoincrement())
  author    String   @db.VarChar(100)
  rating    Int
  comment   String   @db.VarChar(500)
  createdAt DateTime @default(now())

  // Relación N:1 con Producto (Cada reseña pertenece a un producto)
  productId Int
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@map("reviews")
}